server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/comfortable_walk_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate.jdbc.lob.non_contextual_creation: true

logging:
  file: AutoGrubber.log